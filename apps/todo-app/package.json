{
  "name": "@librebase/todo-app",
  "version": "0.1.5",
  "description": "Reference todo app on Librebase \u2014 auth + todos via @librebase/librebase against a lis project API",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "node src/server.mjs",
    "test": "node test/unit.mjs && node test/e2e.mjs && node test/live.mjs"
  },
  "dependencies": {
    "@librebase/librebase": "file:../../packages/sdk"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "GPL-3.0-or-later"
}
