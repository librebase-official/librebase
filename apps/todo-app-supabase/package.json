{
  "name": "@librebase/todo-app-supabase",
  "version": "0.1.0",
  "description": "Same todo app on a real Supabase backend — auth + todos CRUD via /auth/v1 + /rest/v1",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "node src/server.mjs",
    "test": "node test/unit.mjs && node test/e2e-supabase.mjs && node test/bench-crud.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "GPL-3.0-or-later",
  "dependencies": {
    "@librebase/librebase": "file:../../packages/sdk"
  }
}
