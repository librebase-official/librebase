# Trusted OCI runtime seam — extend lic `std/runtime/seam.li` pattern.
# Merged into lic upstream; vendored here for librebase development.

extern proc container_runtime_tag_i() -> int
  requires true
  ensures true
  decreases 0

extern proc container_is_linux_i() -> int
  requires true
  ensures true
  decreases 0

extern proc container_mkdir_p_i(path: var ptr) raises IO -> int
  requires true
  ensures true
  decreases 0

extern proc container_file_exists_i(path: var ptr) -> int
  requires true
  ensures true
  decreases 0

extern proc container_read_file_i(path: var ptr, buf: var ptr, cap: int) raises IO -> int
  requires true
  ensures true
  decreases 0

extern proc container_write_file_i(path: var ptr, data: var ptr) raises IO -> int
  requires true
  ensures true
  decreases 0

extern proc container_remove_file_i(path: var ptr) raises IO -> int
  requires true
  ensures true
  decreases 0

extern proc container_remove_dir_i(path: var ptr) raises IO -> int
  requires true
  ensures true
  decreases 0

extern proc container_getenv_i(name: var ptr, buf: var ptr, cap: int) -> int
  requires true
  ensures true
  decreases 0

extern proc container_env_is_i(name: var ptr, value: var ptr) -> int
  requires true
  ensures true
  decreases 0

extern proc container_json_field_str_i(json: var ptr, key: var ptr, out: var ptr, cap: int) -> int
  requires true
  ensures true
  decreases 0

extern proc container_json_field_int_i(json: var ptr, key: var ptr, out: var ptr) -> int
  requires true
  ensures true
  decreases 0

extern proc container_json_field_bool_i(json: var ptr, key: var ptr, out: var ptr) -> int
  requires true
  ensures true
  decreases 0

extern proc container_state_dir_i(out: var ptr, cap: int) -> int
  requires true
  ensures true
  decreases 0

extern proc container_state_path_i(id: var ptr, out: var ptr, cap: int) -> int
  requires true
  ensures true
  decreases 0

extern proc container_state_write_i(id: var ptr, oci_version: var ptr, status: var ptr, pid: int, has_pid: int, bundle: var ptr, exit_code: int, has_exit: int) raises IO -> int
  requires true
  ensures true
  decreases 0

extern proc container_state_read_i(id: var ptr, json_out: var ptr, cap: int) raises IO -> int
  requires true
  ensures true
  decreases 0

extern proc container_state_delete_i(id: var ptr) raises IO -> int
  requires true
  ensures true
  decreases 0

extern proc container_cgroup_root_i(out: var ptr, cap: int) -> int
  requires true
  ensures true
  decreases 0

extern proc container_cgroup_path_i(id: var ptr, out: var ptr, cap: int) -> int
  requires true
  ensures true
  decreases 0

extern proc container_cgroup_create_i(id: var ptr) raises IO -> int
  requires true
  ensures true
  decreases 0

extern proc container_cgroup_remove_i(id: var ptr) raises IO -> int
  requires true
  ensures true
  decreases 0

extern proc container_cgroup_join_i(id: var ptr) raises IO -> int
  requires true
  ensures true
  decreases 0

extern proc container_cgroup_apply_limits_i(cgroup_path: var ptr, config_json: var ptr) raises IO -> int
  requires true
  ensures true
  decreases 0

extern proc container_namespace_flags_i(config_json: var ptr) -> int
  requires true
  ensures true
  decreases 0

extern proc container_json_process_uid_i(config_json: var ptr) -> int
  requires true
  ensures true
  decreases 0

extern proc container_json_process_gid_i(config_json: var ptr) -> int
  requires true
  ensures true
  decreases 0

extern proc container_state_pid_value_i(state_json: var ptr) -> int
  requires true
  ensures true
  decreases 0

extern proc container_stdout_i(msg: var ptr) raises IO -> int
  requires true
  ensures true
  decreases 0

extern proc container_unshare_i(flags: int) -> int
  requires true
  ensures true
  decreases 0

extern proc container_fork_child_i() -> int
  requires true
  ensures true
  decreases 0

extern proc container_pivot_root_i(rootfs_path: var ptr) -> int
  requires true
  ensures true
  decreases 0

extern proc container_setup_rootfs_i(bundle_path: var ptr, config_json: var ptr) -> int
  requires true
  ensures true
  decreases 0

extern proc container_drop_privileges_i(uid: int, gid: int) -> int
  requires true
  ensures true
  decreases 0

extern proc container_exec_i(cwd: var ptr, config_json: var ptr) -> int
  requires true
  ensures true
  decreases 0

extern proc container_kill_i(pid: int, signal: int) -> int
  requires true
  ensures true
  decreases 0

extern proc container_pid_alive_i(pid: int) -> int
  requires true
  ensures true
  decreases 0

extern proc container_seccomp_apply_i(config_json: var ptr) -> int
  requires true
  ensures true
  decreases 0

extern proc container_eprint_i(msg: var ptr) raises IO -> int
  requires true
  ensures true
  decreases 0

extern proc container_buf_alloc_i(nbytes: int) raises Alloc -> ptr
  requires 0 < nbytes
  ensures true
  decreases 0

extern proc container_buf_free_i(p: var ptr) raises Alloc -> unit
  requires true
  ensures true
  decreases 0

extern proc container_str_cmp_i(a: var ptr, b: var ptr) -> int
  requires true
  ensures true
  decreases 0

extern proc container_path_join_i(a: var ptr, b: var ptr, out: var ptr, cap: int) -> int
  requires true
  ensures true
  decreases 0

extern proc container_bundle_read_config_i(bundle_path: var ptr, buf: var ptr, cap: int) raises IO -> int
  requires 0 < cap
  ensures true
  decreases 0

extern proc container_bundle_rootfs_exists_i(bundle_path: var ptr, config_json: var ptr) -> int
  requires true
  ensures true
  decreases 0

extern proc container_json_status_i(state_json: var ptr, out: var ptr, cap: int) -> int
  requires true
  ensures true
  decreases 0

extern proc container_json_pid_i(state_json: var ptr, out: var ptr, has_pid: var ptr) -> int
  requires true
  ensures true
  decreases 0
