# Librebase vs open stack — 16:9 HyperFrames cut

Same 28s sequence as the 9:16 social cut, laid out for landscape.

```bash
npx hyperframes render . -q high -o ../renders/librebase-benchmark-16x9.mp4
```
