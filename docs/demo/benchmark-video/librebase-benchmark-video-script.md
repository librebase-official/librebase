# Librebase vs open stack — short cut (~45–60s)

Same machine. Same workload. One table.

---

## 0:00–0:08 — Hook

**VO:** "What if the open stack ran at a fraction of the cost — and still beat it on every number that matters?"

**On screen:** Librebase wordmark. Subtitle: *Same Mac. Same tests. Honest numbers.*

---

## 0:08–0:18 — Footprint

**VO:** "Twelve containers. Almost two gigs of RAM. Or one container, two megabytes, ready in a quarter second."

**On screen:**

| | Open stack | Librebase |
|---|---|---|
| Containers | 12 | **1** |
| RAM idle | ~1.85 GB | **~2 MB** |
| Cold start | seconds | **265 ms** |

---

## 0:18–0:32 — Speed

**VO:** "Faster auth. Faster storage. Faster queries. And vector search — four thousand six hundred queries a second, at a hundred percent recall. That's nine times faster than the open stack's vector path, at the same accuracy."

**On screen (key lines only):**

| | Open stack | Librebase |
|---|---|---|
| Auth login | 196 ms | **52 ms** |
| Storage upload | 24 ms | **2.8 ms** |
| Vector QPS (100% recall) | 499 | **4,683** |

---

## 0:32–0:45 — Close

**VO:** "Librebase — tiny, high-performance backend as a service for AI. Start now."

**On screen:** Librebase wordmark · *Tiny, high-performance backend as a service for AI* · **Start now** pill.

---

## Notes

- Length: ~45s (social) or stretch VO pauses to ~60s (landing).
- Tone: calm, confident, no jargon dumps.
- Competitor label on screen/VO: **open stack** (not the product brand).
- Honesty line stays on screen at the end; don't over-explain gaps in VO.
- Full numbers: `benchmarks/full-stack/results/` and `vector-fresh-2026-08-13.json`.
- Motion board: `storyboard.html`.
